apiVersion: v1
kind: Service
metadata:
  name: echoheaders
  labels:
    app: echoheaders
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: echoheaders
---
apiVersion: v1
kind: ReplicationController
metadata:
  name: echoheaders
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: echoheaders
    spec:
      imagePullSecrets:
      - name: aliregistrykey
      containers:
      - name: echoheaders
        image: hub.docker.vpclub.cn/vpclub_containers/echoserver:1.4
        ports:
        - containerPort: 8080
