- name: CoreOS | Check if bootstrap is needed
  raw: stat {{ bootstrap_script_dir }}/.bootstrapped
  register: need_bootstrap
  ignore_errors: True

- include: coreos_bootstrap.yml
  when: need_bootstrap | failed

