apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-kubernetes-submit-queue
  name: k8s-kubernetes-sq-status
spec:
  ports:
  - name: status
    port: 80
    targetPort: status
  selector:
    app: k8s-kubernetes-submit-queue
  type: NodePort
