kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: k8s-kubernetes-submit-queue
  name: k8s-kubernetes-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
