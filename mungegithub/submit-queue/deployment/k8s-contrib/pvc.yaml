kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
    app: k8s-contrib-submit-queue
  name: k8s-contrib-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
