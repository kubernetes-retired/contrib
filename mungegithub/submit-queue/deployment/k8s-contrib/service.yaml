apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-contrib-submit-queue
  name: k8s-contrib-sq-status
spec:
  ports:
  - name: status
    port: 80
    targetPort: status
  selector:
    app: k8s-contrib-submit-queue
  type: NodePort
