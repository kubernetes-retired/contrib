apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app: k8s-docs-submit-queue
  name: k8s-docs-cache
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  gcePersistentDisk:
    pdName: k8s-docs-submit-queue
    fsType: ext4
