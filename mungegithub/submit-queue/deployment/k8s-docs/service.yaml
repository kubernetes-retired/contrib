apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-docs-submit-queue
  name: k8s-docs-sq-status
spec:
  ports:
  - name: status
    port: 80
    targetPort: status
  selector:
    app: k8s-docs-submit-queue
  type: NodePort
